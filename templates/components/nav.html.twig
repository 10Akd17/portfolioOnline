<nav class="navbar navbar-expand-lg ">
    <div class="container-fluid">
        <a class="navbar-brand " href="{{ path('app_home') }}">A K D.</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    {# <a class="nav-link active text-white" aria-current="page"
                        href="{{ path('app_article') }}">Ajouter article</a> #}
                </li>

                {# <li class="nav-item">
                    <a class="nav-link text-white" href="{{ path('app_experience') }}">Mes Expériences</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white" href="{{ path('app_competence') }}">Mes compétences </a>
                </li> #}
                
                <li class="nav-item">
                    <a class="nav-link text-white" href="{{ path('app_contact') }}">Contact</a>
                </li>
                

                {% if is_granted ('ROLE_ADMIN') %}
                 <li class="nav-item dropdown">
                 <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                     Admin
                 </a>

              <ul class="dropdown-menu" aria-labelledby="navbarDropdown">

              {% if app.user %}

                 <li><a class="dropdown-item" href="{{ path('app_contact_messagerie') }}">Messagerie</a></li>
                 <li><a class="dropdown-item" href="{{ path('app_projet_ajout') }}">Ajouter une projet</a></li>
                 <li><a class="dropdown-item" href="{{ path('app_experience_ajout') }}">Ajouter une experience</a></li>
                 <li><a class="dropdown-item" href="{{ path('app_competence_ajout') }}">Ajouter une competence</a></li>
                <li><hr class="dropdown-divider"></li>
                <li><a class="dropdown-item"  href="{{path('app_logout')}}">Deconnexion</a></li>
                {% endif %}
             </ul>
             </li>
        {% endif  %}
              
            </ul>

        </div>
    </div>
</nav>